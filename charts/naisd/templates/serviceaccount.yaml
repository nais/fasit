{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "naisd.serviceAccountName" . }}
  labels:
    {{- include "naisd.labels" . | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: naisd-user@{{ .Values.envProjectId }}.iam.gserviceaccount.com
  {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
