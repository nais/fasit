apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "naisd.fullname" . }}
  labels:
    {{- include "naisd.labels" . | nindent 4 }}
  annotations:
    cluster-autoscaler.kubernetes.io/safe-to-evict: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "naisd.selectorLabels" . | nindent 6 }}
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "naisd.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "naisd.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: NAIS_SA_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.serviceAccountName
          args:
            - --production
            - --tenant-name
            - {{ .Values.tenantName }}
            - --env
            - {{ .Values.env }}
            - --env-project-id
            - {{ .Values.envProjectId }}
            - --nais-project-id
            - {{ .Values.naisProjectId }}
            - --management={{ .Values.management }}
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http

          volumeMounts:
          - mountPath: /tmp
            name: tmp
          - mountPath: /.kube
            name: kubeconfig
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

      volumes:
      - name: tmp
        emptyDir: {}
      - name: kubeconfig
        emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
