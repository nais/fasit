apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "naisd.fullname" . }}-test-connection"
  labels:
    {{- include "naisd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "naisd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
