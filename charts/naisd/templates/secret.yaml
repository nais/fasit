{{ if .Values.google.useServiceAccountKey }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-google-sa-key
data:
  key.json: {{ .Values.google.serviceAccountKey | b64enc | quote }}
{{ end }}
