environmentKinds:
  - tenant
  - management
  - onprem
  - legacy
values:
  env:
    displayName: Env
    computed:
      template: "{{.Env.name}}"
  envProjectId:
    displayName: Env project id
    computed:
      template: "{{.Env.project_id}}"
  deploySubscription:
    displayName: Subscription to fetch deploy instructions from
    computed:
      template: "naisd-fasit-{{.Env.name}}"
  google.serviceAccountKey:
    displayName: Use service account key?
    computed:
      template: "{{ .Env.naisd_key | quote }}"
  google.useServiceAccountKey:
    displayName: Use service account key?
    computed:
      template: '{{ eq .Kind "onprem" }}'
  https_proxy:
    displayName: HTTPS proxy
    config:
      type: string
    ignoreKind:
      - tenant
      - management
      - legacy
  image.tag:
    displayName: Image tag
    config:
      type: string
  management:
    displayName: Management Cluster?
    computed:
      template: '"{{eq .Kind "management"}}"'
  no_proxy:
    displayName: No proxy
    config:
      type: string
    ignoreKind:
      - tenant
      - management
      - legacy
  tenantName:
    displayName: Tenant name
    computed:
      template: "{{.Tenant.Name}}"
