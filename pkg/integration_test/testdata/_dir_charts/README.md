Use this directory to define charts that are used for testing.
The charts in this directory will be packed when requested by the test suite.

Use `oci://name` and define version to request `<name>-<version>` from this directory.

> [!IMPORTANT]
> The version in the directory name must match the one defined in `Chart.yaml`
