-- Create some configuration values that shouldn't be renamed, because the target key already exists from the earlier steps

INSERT INTO configurations_environment (feature, key, value, environment_id) VALUES ('rename', 'nested.key', '"oldVal"', '{{ .Tenant.tenant1.Env.management.ID }}');

RETURNS

[]
