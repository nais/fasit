chart: oci://europe-north1-docker.pkg.dev/nais-io/nais/dashboard-monitor
source: https://github.com/nais/dashboard/tree/main/charts/dashboard-monitor
version: 0.1.0
environmentKinds:
  - "onprem"
  - "tenant"
  - "legacy"
dependsOn:
  - allOf:
      - "nais-crds"
rolloutSource:
  - org: nais
    repo: dashboard
config:
  image.tag:
    type: string
    displayName: "image tag"
mapping:
  managementProjectID:
    value: "{{ .Management.project_id }}"
    displayName: "Management Project ID"
  projectID:
    value: "{{ .Env.project_id }}"
    displayName: "Project ID"
  env:
    value: "{{ .Env.name }}"
    displayName: "Environments"
