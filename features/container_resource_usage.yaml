chart: oci://europe-north1-docker.pkg.dev/nais-io/nais/container_resource_usage
source: https://github.com/nais/container_resource_usage/tree/master/charts/container_resource_usage
version: 1.0.0
environmentKinds:
  - "tenant"
  - "onprem"
mapping:
  googleProjectID:
    value: "{{ .Env.project_id }}"
  pubsubSubscriptionName:
    value: "{{ .Env.pubsub_subscription_name }}"
config:
  image.tag:
    type: string
    displayName: Image tag
  master:
    type: bool
    displayName: