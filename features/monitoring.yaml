chart: oci://europe-north1-docker.pkg.dev/nais-io/nais/monitoring
version: 0.1.0
config:
  prometheus.server.persistentVolume.size: string
  prometheus.server.ingress.enabled: bool
  prometheus.server.ingress.hosts: string_array
  prometheus.server.baseURL: string
  prometheus.alertmanager.persistentVolume.size: string
  prometheus.alertmanager.ingress.enabled: bool
  prometheus.alertmanager.ingress.hosts: string_array
  prometheus.alertmanager.baseURL: string
  grafana.persistence.size: string
  grafana.ingress.enabled: bool
  grafana.ingress.hosts: string_array
