chart: oci://europe-north1-docker.pkg.dev/nais-io/nais/azurerator
source: https://github.com/nais/azurerator/tree/master/charts
version: 0.1.0
repo: ""
environmentKinds:
  - "tenant"
  - "onprem"
dependsOn:
  - allOf:
      - "nais-crds"
      - "aivenator"
      - "kafkarator"
config:
  trygdeetaten.enabled:
    type: bool

  image.tag:
    type: string

  secret.kafka.enabled:
    type: bool
    displayName: "Enable Kafka"
    description: "Allow synchronization of events between Azurerator instances."

  secret.azure.auth.client-secret:
    type: string
    secret: true
    required: true
    displayName: "Azure client secret"

mapping:
  secret.azure.cluster-name:
    value: "{{.Env.name}}"
    displayName: "Cluster name"
  webproxy:
    template: '{{eq .Kind "onprem"}}'
