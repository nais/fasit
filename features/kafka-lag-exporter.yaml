chart: oci://europe-north1-docker.pkg.dev/nais-io/nais/kafka-lag-exporter
source: https://github.com/nais/helm-charts/tree/main/charts/kafka-lag-exporter
version: 0.4.7
description: Kafka Lag Exporter exposes consumer offset metrics and is used by teams to track consumer lag for Aiven Kafka clusters
environmentKinds:
  - "tenant"
  - "legacy"
dependsOn:
  - allOf:
      - "aivenator"
autoInstall:
  - tenant
  - legacy
mapping:
  kafka-lag-exporter.clusters:
    template: |
      - name: "{{ .Env.aiven_project }}"
        bootstrapBrokers: "{{ .Env.kafka_bootstrap_broker }}"
        consumerProperties:
          security.protocol: "SSL"
          ssl.truststore.location: "/var/run/secrets/nais.io/aiven-credentials/client.truststore.jks"
          ssl.truststore.password: "changeme"
          ssl.keystore.type: "PKCS12"
          ssl.keystore.location: "/var/run/secrets/nais.io/aiven-credentials/client.keystore.p12"
          ssl.keystore.password: "changeme"
          ssl.key.password: "changeme"
        adminClientProperties:
          security.protocol: "SSL"
          ssl.truststore.location: "/var/run/secrets/nais.io/aiven-credentials/client.truststore.jks"
          ssl.truststore.password: "changeme"
          ssl.keystore.type: "PKCS12"
          ssl.keystore.location: "/var/run/secrets/nais.io/aiven-credentials/client.keystore.p12"
          ssl.keystore.password: "changeme"
          ssl.key.password: "changeme"
config:
  kafka-lag-exporter.kafkaClientTimeoutSeconds:
    displayName: "kafka client timeout"
    description: "kafka client timeout in seconds"
    type: int
