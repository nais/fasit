environmentKinds:
  - "tenant"

timeout: 10m

dependencies:
  - any: ["feature1", "feature2"]
  - all: ["feature3", "feature4"]

values:
  gcp:
    displayName: "GCP"
    description: "Running in GCP or not"
    required: true
    computed:
      template: '"{{ne .Kind "onprem"}}"'
    config:
      type: "bool"
      secret: true
