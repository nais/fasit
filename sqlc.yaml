version: "2"
rules:
  - name: "always-order-by"
    rule: |
      query.cmd == "many" && !query.sql.contains("ORDER BY")
    message: |
      SELECT statements must have an ORDER BY clause
sql:
  - &default_domain
    name: "Deployment SQL"
    engine: "postgresql"
    schema: "internal/database/migrations"
    queries: "internal/deployment/queries"
    rules:
      - "always-order-by"
    gen:
      go: &default_go
        package: "deploymentsql"
        out: "internal/deployment/deploymentsql"
        sql_package: "pgx/v5"
        emit_interface: true
        omit_sqlc_version: true
        emit_empty_slices: true
        omit_unused_structs: true
        emit_pointers_for_null_types: true
        # emit_result_struct_pointers: true
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"
          - db_type: "uuid"
            go_type: "*github.com/google/uuid.UUID"
            nullable: true
          - db_type: "environment_kind"
            go_type: "github.com/nais/fasit/internal/database/types.EnvironmentKind"
          - db_type: "environment_kind"
            go_type: "*github.com/nais/fasit/internal/database/types.EnvironmentKind"
            nullable: true
          - column: "environments.labels"
            go_type: "github.com/nais/fasit/internal/database/types.EnvironmentLabels"
          - column: "deployments.target"
            go_type: "github.com/nais/fasit/internal/database/types.EnvironmentLabels"
  - name: "UI SQL"
    !!merge <<: *default_domain
    queries: "internal/ui/database/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "sqlgen"
        out: "internal/ui/database/sqlgen"
  - name: "Environment Management SQL"
    !!merge <<: *default_domain
    queries: "internal/environmentmanagement/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "sqlgen"
        out: "internal/environmentmanagement/sqlgen"
  - name: "Public API SQL"
    !!merge <<: *default_domain
    queries: "internal/api/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "sqlgen"
        out: "internal/api/sqlgen"
  - name: "Environment SQL"
    !!merge <<: *default_domain
    queries: "internal/environment/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "environmentsql"
        out: "internal/environment/environmentsql"
  - name: "Naisd status SQL"
    !!merge <<: *default_domain
    queries: "internal/naisdstatus/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "naisdstatussql"
        out: "internal/naisdstatus/naisdstatussql"
  - name: "Feature SQL"
    !!merge <<: *default_domain
    queries: "internal/feature/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "featuresql"
        out: "internal/feature/featuresql"
  - name: "Audit SQL"
    !!merge <<: *default_domain
    queries: "internal/audit/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "auditsql"
        out: "internal/audit/auditsql"
  - name: "Reconciler SQL"
    !!merge <<: *default_domain
    queries: "internal/reconciler/queries"
    gen:
      go:
        !!merge <<: *default_go
        package: "reconcilersql"
        out: "internal/reconciler/reconcilersql"
