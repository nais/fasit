type Feature {
  name: String!
  chart: String!
  version: String!
  repo: String!
  source: String!
  dependsOn: [String!]!
  config: RawMessage!
  environmentKinds: [EnvironmentKind!]!
}

extend type Query {
  features(kind: EnvironmentKind): [Feature!]!
}
